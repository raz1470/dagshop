.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/
.coverage*
htmlcov/
.DS_Store

# Internal working log, kept locally, not published
NOTES.md
notes_archive/

# Scoping/preferences docs: internal, kept local, never committed
# (Decision, 2026-09-09: Ryan chose to keep these local rather than
# version-controlled.)
SCOPE.md
PREFERENCES.md

# Scratch preview and mockup output, rebuilt each session
Claude outputs/

# Working data/exports for local sessions; folders themselves are tracked
inputs/*
outputs/*
!inputs/.gitkeep
!outputs/.gitkeep
